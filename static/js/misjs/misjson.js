"use strict";

var url = window.location;
var baseUrl = url.protocol + "//" + url.host + "/" + url.pathname.split('/')[1]+ "/" ;//+ url.pathname.split('/')[2];
//var baseUrl = url.protocol + "//" + url.host  ;// + "/" + url.pathname.split('/')[1]+ "/" + url.pathname.split('/')[2];

$(document).ready(

  //  alert(baseUrl)
)